# MCP Server Dependencies
mcp>=1.0.0

# Parent package dependencies (inherited from ai_orchestrator)
# These are already in the parent requirements.txt but listed for completeness
openai>=1.0.0
anthropic>=0.18.0
google-genai>=1.0.0
python-dotenv>=1.0.0
rich>=13.0.0
pydantic>=2.0.0
requests>=2.31.0
